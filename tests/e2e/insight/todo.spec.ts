// var until = require('selenium-webdriver').until
// var driver = browser.driver

// return driver.wait(
//   until.elementLocated(by.css('.foo')), 2000)
//   .then(e => {
//     element(by.css('.bar')).click()
// }, 1000);


// it('should allow users to search for insights', () => {})


// it('should allow users like insights', () => {})


// it('should allow users to view other user\'s profiles', () => {})
// it('should allow users to logout', () => {})
// it('upload cms insight and make a comment from fuel)

// it('user should be able to see and answer polls', () => {})
